{% extends 'base.html.twig' %}
{% block body %}
    <b>Add/Edit Group</b>

    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_row(form.name) }}
    {{ form_row(form.roles) }}

    <button type="submit">Save</button>
    {{ form_end(form) }}
{% endblock %}